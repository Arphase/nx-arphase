import { CompanySelectDirective } from './company-select.directive';

describe('CompanySelectDirective', () => {
  it('should create an instance', () => {
    const directive = new CompanySelectDirective();
    expect(directive).toBeTruthy();
  });
});
