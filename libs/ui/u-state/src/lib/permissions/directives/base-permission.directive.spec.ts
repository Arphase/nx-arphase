import { BasePermissionDirective } from './base-permission.directive';

describe('BasePermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new BasePermissionDirective();
    expect(directive).toBeTruthy();
  });
});
