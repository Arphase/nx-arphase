import { EditableRevisionPipe } from './editable-revision.pipe';

describe('EditableRevisionPipe', () => {
  it('create an instance', () => {
    const pipe = new EditableRevisionPipe();
    expect(pipe).toBeTruthy();
  });
});
