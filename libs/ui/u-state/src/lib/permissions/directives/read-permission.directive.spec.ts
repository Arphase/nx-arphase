import { ReadPermissionDirective } from './read-permission.directive';

describe('ReadPermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new ReadPermissionDirective();
    expect(directive).toBeTruthy();
  });
});
