import { SumAllPlacesPipe } from './sum-all-places.pipe';

describe('SumAllPlacesPipe', () => {
  it('create an instance', () => {
    const pipe = new SumAllPlacesPipe();
    expect(pipe).toBeTruthy();
  });
});
