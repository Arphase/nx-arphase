import { CreatePermissionDirective } from './create-permission.directive';

describe('CreatePermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new CreatePermissionDirective();
    expect(directive).toBeTruthy();
  });
});
