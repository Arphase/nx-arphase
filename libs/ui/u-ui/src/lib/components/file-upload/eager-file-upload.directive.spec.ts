import { EagerFileUploadDirective } from './eager-file-upload.directive';

describe('EagerFileUploadDirective', () => {
  it('should create an instance', () => {
    const directive = new EagerFileUploadDirective();
    expect(directive).toBeTruthy();
  });
});
