import { DeletePermissionDirective } from './delete-permission.directive';

describe('DeletePermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new DeletePermissionDirective();
    expect(directive).toBeTruthy();
  });
});
